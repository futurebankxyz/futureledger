"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("../decorators/command");
const models_1 = require("@vuesion/models");
let Prettier = class Prettier {
    async run(args) {
        if (this.staged) {
            prettyQuick(args);
        }
        else {
            prettier(args, this.pattern);
        }
    }
};
Prettier = __decorate([
    command_1.Command({
        name: 'prettier',
        description: 'Format project files.',
        options: [
            { flags: '-p, --pattern <pattern>', description: 'Match file pattern' },
            { flags: '-s, --staged', description: 'O≈nly prettify staged files' },
        ],
    })
], Prettier);
exports.Prettier = Prettier;
const prettier = (args, pattern) => {
    args = [
        '--config',
        '.prettierrc',
        '--ignore-path',
        '.prettierignore',
        '--write',
        pattern ? pattern : `./**/*.{${models_1.VuesionConfig.prettier.extensions}}`,
    ];
    process.argv = process.argv.concat(args);
    require('prettier/bin-prettier.js');
};
const prettyQuick = (args) => {
    args = ['--staged'].concat(args);
    process.argv = process.argv.concat(args);
    require('pretty-quick/bin/pretty-quick.js');
};
