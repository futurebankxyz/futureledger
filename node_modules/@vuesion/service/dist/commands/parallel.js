"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("../decorators/command");
const dist_1 = require("@vuesion/utils/dist");
const concurrently = require('concurrently');
let Add = class Add {
    async run(args) {
        try {
            await concurrently(args, { killOthers: ['failure'], prefix: '[{command}]', prefixLength: 28 });
        }
        catch (e) {
            dist_1.handleProcessError({ code: 1, trace: 'Please see the console output above for a detailed error description.' });
        }
    }
};
Add = __decorate([
    command_1.Command({
        name: 'parallel [commands...]',
        alias: 'p',
        description: 'Run commands in parallel.',
    })
], Add);
exports.Add = Add;
