import { IVuexPersistStorage } from './vuex-persist';
export declare class PersistLocalStorage implements IVuexPersistStorage {
    private readonly localBeforePersist;
    modules: string[];
    prefix: string;
    length: number;
    forceInitialState: boolean;
    [key: string]: any;
    [index: number]: string;
    constructor(modules?: string[], beforePersist?: (state: any) => any, prefix?: string);
    private getKey;
    clear(): void;
    getItem(key: string): string | null;
    key(index: number): string | null;
    removeItem(key: string): void;
    setItem(key: string, data: string): void;
    beforePersist(state: any): any;
}
