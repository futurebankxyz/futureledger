import { Spinner } from './ui';
export interface IProcessOptions {
    cwd?: string;
    silent?: boolean;
    debug?: boolean;
}
export interface IProcessError {
    code: number;
    trace: string;
}
export declare const runProcess: (name: string, args?: string[], options?: IProcessOptions) => Promise<any>;
export declare const handleProcessError: (err: IProcessError, spinner?: Spinner) => void;
